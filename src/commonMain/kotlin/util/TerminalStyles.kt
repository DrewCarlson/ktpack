package ktpack.util

import com.github.ajalt.mordant.rendering.TextColors.*
import com.github.ajalt.mordant.rendering.TextStyles.*

val title = (bold + underline + white)
val verbose = (bold + white)
val info = (bold + brightBlue)
val warn = (bold + brightYellow)
val success = (bold + brightGreen)
val failed = (bold + brightRed)
